# Changelog

## 2.1.0
- Merged `main.py` and `youtube_main.py` into a single entry point with `--tg` / `--yt` flags
- Added `--only-replies` and `--only-top` for YouTube comment type filtering
- Reorganized source code into `src/` package

## 2.0.0
- Added YouTube Data API v3 support
- Unified data format for Telegram and YouTube

## 1.0.0
- Initial release: Telegram comment collection, LLM toxicity and political analysis
